\ProvidesPackage{FoxyByteTemplate}		%nome del pacchetto

%-----package per i margini---
\usepackage[top=3.2cm, bottom=3.2cm, left=2.8cm, right=2.8cm]{geometry}

%----package per l'appendice----
\usepackage{appendix}

%------COPERTINA DI OGNI DOCUMENTO------
\newcommand{\copertina}{
	\begin{titlepage}
		\begin{center}
			
			\begin{center}
				\centerline{\includegraphics[scale=0.90]{../Logo/FoxyByteLogo.png}}
			\end{center}
			
			\vspace{1cm}
			
			\begin{Huge}
				\textbf{\Titolo{}} \\
			\end{Huge}
			
			\vspace{9pt}  
			
			\begin{large}
				\textbf{\Gruppo{}}\ - \textbf{\NomeProgetto{}}\\
				\vspace{6pt}
				\Mail{}
			\end{large}	  
			
			\vspace{15pt}
			
			\bgroup
			\def\arraystretch{1.3}
			\centering
			\begin{tabular}{r|L{5cm}}
				\multicolumn{2}{c}{\textbf{Informazioni sul documento} } \\ \hline
				\textbf{Versione} & \VersioneDocumento{} \\
				\textbf{Redazione} & \Redazione{} \\
				\textbf{Verifica} & \Verifica{} \\ 
				\textbf{Uso} & \Uso \\
				\textbf{Distribuzione} & \Distribuzione{}
			\end{tabular}
			\egroup
			
			\vspace*{\fill}
			
			\begin{center}
				\textbf{Descrizione\\}
				\DescrizioneDoc{}
			\end{center}
			
		\end{center}
	\end{titlepage}
}


%------INTESTAZIONE E PIE PAGINA------
\usepackage{lastpage} %info sul # dell'ultima pagina del documento
\usepackage{fancyhdr} %per modificare dimensioni,margini, intestazioni e righe a piè di pagina
\fancypagestyle{plain}{
    % cancella tutti i campi di intestazione e piè di pagina
    \fancyhf{}

    \lhead{ %in alto a sx
    \includegraphics[scale=0.1]{../Logo/FoxyByteLogo.png}
    }

    \rhead{
        \Mail{}
    }

    \lfoot{ %piè di pagina a sx
        \Titolo{} \ - \textit{\Gruppo{}}
    }
    \rfoot{Pagina \thepage{} di \pageref{LastPage}} %es: pag: 4 di 10

    %linea orizzontale alle posizioni top e bottom della pagina (se è 0, non c'è la linea)
    \renewcommand{\headrulewidth}{0.3pt}  
    \renewcommand{\footrulewidth}{0.3pt}
}
\pagestyle{plain}


%Variabili per i colori
\usepackage[usenames, dvipsnames, table]{xcolor}
\definecolor{darkgreen}{RGB}{49, 135, 23}
\definecolor{bluegray}{rgb}{0.4, 0.6, 0.8}
\definecolor{beaublue}{rgb}{0.74, 0.83, 0.9}
\definecolor{azure}{rgb}{0.94, 1.0, 1.0}
\newcommand{\verde}[1]{\textcolor{darkgreen}{#1}}
\newcommand{\rosso}[1]{\textcolor{red}{#1}}

%-----package per la LINGUA---- 
\usepackage[english, italian]{babel} %l'ultima lingua è quella generale
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

%------package per le IMMAGINI-----
\usepackage{graphicx}

%------package per le TABELLE-----
\usepackage{tabularx} %tabelle larghe
\usepackage{longtable} %una tabella può continuare su più pagine
\usepackage{booktabs} %toprule e midrule usati al posto di hline
\usepackage{multirow} %utile per visualizzare una cella su più righe
\usepackage{float}
\usepackage{array}
\newcolumntype{L}[1]{>{\raggedright\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\newcolumntype{C}[1]{>{\centering\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\newcolumntype{R}[1]{>{\raggedleft\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}

%------package per i PDF-----
\usepackage{pdfpages}

%-----packeage per i link----
\usepackage{url}
\usepackage[colorlinks=true]{hyperref}
\hypersetup{
    colorlinks=true,
    citecolor=black,
    filecolor=black,
    linkcolor=black, % colore dei link interni
    urlcolor=Maroon  % colore dei link interniesterni
}

%------ paragrafi ---
\setcounter{secnumdepth}{5} % numera i sottoparagrafi
\setcounter{tocdepth}{5} % aggiunge all'indice i sottoparagrafi
%\newcommand{\aCapo}{ ~ \vspace{0.25cm} \\} % per andare a capo dopo paragraph e subparagraph
\makeatletter
\renewcommand\subparagraph{\@startsection{subparagraph}{5}{\z@}{-2ex\@plus -1ex}{0ex}{\normalfont\normalsize\bfseries}}
\makeatother%{0.0001pt \@plus .2ex}{-3.25ex\@plus -1ex \@minus -.2ex}

%---- simbolo euro ---
\usepackage{eurosym}

%-----per il glossario-----
\newcommand{\gloSection}[1]{\subsection*{#1} \addcontentsline{toc}{subsection}{#1}}

% ----per il codice -----
\definecolor{backGroundGreen}{RGB}{204, 255, 204}
\definecolor{backGroundGrey}{RGB}{242, 242, 242}

\definecolor{commandGreen}{RGB}{0, 153, 0}

\usepackage{listings}
\lstset{
	backgroundcolor=\color{backGroundGrey},
	basicstyle=\ttfamily,
	columns=flexible,
	breaklines=true,
	classoffset=1,
	keywordstyle=\color{blue},
	morekeywords={etherless},
	% classoffset=2,
	% keywordstyle=\color{commandGreen},
	% morekeywords={help, init, run, deploy, search, createConfig, delete, logout, list}
}